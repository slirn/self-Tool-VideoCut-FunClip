# REQ-20260918-045 — 粗剪合成由可选改为必做

## 背景与目标

粗剪合成（REQ-20260916-016）上线初期作为可选步骤（用户可能直接到优化字幕阶段）。运行一段时间后该阶段已成为标准流程的一部分，「可选」字样会让用户误以为可跳过。本需求去除「可选」字样和徽章。

## 验收

| # | 标准 | 状态 |
|---|---|---|
| 1 | 工作台阶段列表中「粗剪合成」无「可选」徽章 | ✅ |
| 2 | 阶段描述改为「必做阶段」 | ✅ |
| 3 | 函数 docstring 更新 | ✅ |

## 涉及文件

- `slirn_home/app.py` — `_WB_STAGES` 中粗剪合成的 desc / `_render_workbench` 中 `opt_chip = ""`（REQ-20260918-045）
- `tests/test_workbench.py` — 断言改为 `slirn-wb-stage-optional not in html`

## E2E

`work/REQ-20260918-040/_e2e_040_046.py` 045 段：2/2 通过。
