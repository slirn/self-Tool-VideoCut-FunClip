# REQ-20260915-001 — 处理剪辑阶段（一）：字幕生成 + 对照校对

| 字段 | 值 |
|---|---|
| 编号 | REQ-20260915-001 |
| 日期 | 2026-09-15 |
| 优先级 | P1（处理剪辑阶段第一步） |
| 状态 | ✅ 已实现 |
| 关联 | Epic REQ-20260914-001（任务体系）；REQ-20260914-002（上传/截取修复） |
| 改动范围 | `slirn_home/`（新增 asr_service + 详情页处理区）；tasklib 零改动；funclip/ 零改动 |

---

## 0. 背景

任务创建后（上传 → 截取 → 热词），进入**处理剪辑阶段**。第一步：调用上游 FunClip
的开源字幕生成能力（FunASR seaco-paraformer + 热词），把识别结果以**列表**形式展示，
并支持**逐段对照原视频校对**——确认时间戳与视频时段对应、内容一致。

## 1. 核心需求

| ID | 需求 |
|---|---|
| REQ-1.1 | 任务详情页新增「处理区」：对任务视频（有截取段用截取段，否则用原视频）+ 任务热词，一键调用 FunClip 字幕生成 |
| REQ-1.2 | 长任务后台执行 + 前端轮询进度（阶段：加载模型 → 提取音频 → 识别中 → 保存），不阻塞页面 |
| REQ-1.3 | 结果落盘：`tasks/<id>/outputs/subtitle.srt`（可复用）+ `subtitle.json`（结构化段列表） |
| REQ-1.4 | 生成成功后任务状态迁移为 `SUBTITLE_GENERATED`（tasklib 状态机既有节点） |
| REQ-1.5 | 字幕列表 UI：序号 / 起止时间(HH:MM:SS,mmm) / 文本；点击任一段 → 播放器定位到该段起点自动播放 |
| REQ-1.6 | 播放中当前字幕行高亮跟随（timeupdate 同步），用于目视校对时间对应关系 |
| REQ-1.7 | 「播放截取段」按钮（既有占位、此前无 JS 处理）一并接通 |
| REQ-1.8 | 支持重新生成（覆盖 outputs，updated_at 刷新） |
| REQ-1.9 | 无音频轨 / 文件缺失 / 模型加载失败等错误在状态区明确提示，不崩页面 |

## 2. 验收标准

- [x] AC-1 任务详情页可见「🎙 生成字幕」，点击后进度可见（阶段文案 + 已耗时）
- [x] AC-2 完成后字幕列表渲染（≥1 段），时间格式 HH:MM:SS,mmm
- [x] AC-3 点击字幕行 → 播放器从该段开始播放，行高亮
- [x] AC-4 播放过程中高亮随时间推进移动
- [x] AC-5 `outputs/subtitle.srt` + `subtitle.json` 生成，srt 与列表一致
- [x] AC-6 任务状态 → 已生成字幕；任务列表徽标同步
- [x] AC-7 生成中重复点击提示"已在生成中"，不重复起任务
- [x] AC-8 既有 51 个 pytest 全过 + ruff 0 错
- [x] AC-9 真实视频端到端验证（含热词任务）

## 3. 非目标

- ❌ 不改 `funclip/` 上游源码（只 import 复用 VideoClipper）
- ❌ 不改 tasklib（status 枚举里 SUBTITLE_GENERATED 已存在）
- ❌ 不做字幕编辑/修改（后续阶段）、不做 LLM 整理、不做说话人分离

## 4. 验证记录（2026-09-15）

- **单测**：67 passed（51 既有 + 16 新增 `tests/test_asr_service.py`），ruff 0 错
- **E2E（真实模型）**：`work/REQ-20260915-001-subtitle-stage/_e2e_test.py`
  - 上传 2 分钟语音视频 → 截取 60s → 建任务（热词：IP影视/训练营/讲师）→ 后台识别 ~16s → 29 段
  - 内容与视频语音吻合（"hello hello" / "大家好呀" / "我们稍等一下"）
  - `subtitle.srt`（29 个 `-->`）与 `subtitle.json` 29 段一致；status → SUBTITLE_GENERATED
  - Range 请求 206 + Content-Range 校验通过
- **浏览器（CDP 无头）**：`_browser_sub_test.py` → 任务 20260915-004
  - 列表 29 行、🔄 重新生成字幕、播放截取段按钮均在
  - 点击第 5 行（17.16s）→ 1s 后 t=17.91s 高亮第 5 行（行内命中）
  - 4s/8s 后 t=20.92s/25.41s 高亮仍第 5 行 —— 第 5→6 行之间为 17.8s~29.6s 真实静音段，
    **粘滞高亮**（间隙保持上一行）为预期校对行为；29.55s 起切第 6 行
- **顺手修复的存量 bug**：cut_preview 迁移缓存目录后 create_task 只扫 `.temp/`，
  截取段文件从未入库 → 悬挂 segment 静默回退全片识别。已修：create_task 双路径扫描
  + `gen_subtitle` 入口 `_ensure_segment_file` 自愈重截。
